# Django + Solana Wallet Login 🚀

**One-click Phantom wallet connect for Nigerian Web3 apps — built with Django backend, zero React bloat.**

Perfect for devs shipping with Django **device-swapping marketplaces**, or **Solana DID dashboards**.

---

## ✨ Features
-  (views + URLs + HTML/JS
- Connects **Phantom** wallet in 1 click
- Sends public key to Django backend
- Ready for **signature verification** 
- Works offline-first (no npm, no webpack)

---

## 🚀 Quick Start

```bash
# 1. Clone
git clone https://github.com/Banchi-salim/sol_django_login.git
cd sol_django_login

# 2. Install
pip install django solana
pip install PyNaCl

# 3. Run
python manage.py runserver

# Django + Solana Wallet Login (12 Lines) 🚀

**One-click Phantom wallet connect for Nigerian Web3 apps — built with Django backend, zero React bloat.**

Perfect for Naija devs shipping **device-swapping marketplaces**, **$HIPO airdrop claimers**, or **Solana DID dashboards** in Abuja, Lagos, or PH.

---

## ✨ Features
- **12 lines of real code** (views + URLs + HTML/JS)
- Connects **Phantom** wallet in 1 click
- Sends public key to Django backend
- Ready for **signature verification** (add Anchor later)
- Works offline-first (no npm, no webpack)

---

## 🚀 Quick Start

```bash
# 1. Clone
git clone https://github.com/banchi-salim/django-solana-login-template.git
cd django-solana-login-template

# 2. Install
pip install django solana

# 3. Run
python manage.py runserver
